import { IMobileFontConfig } from "./mobile-font-config";

export interface IMobileConfig {
    mobileNavWidth: number,
    useMainMenu: boolean,
    useFontSize: boolean,
    fontConfig: IMobileFontConfig
}